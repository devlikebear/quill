# Anthropic Authentication (Choose ONE)
# ==========================================

# Option 1: API Key (Recommended for production)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxx

# Option 2: Claude Code Subscription
# Use credentials from Claude Code installation
# ANTHROPIC_HOME=/Users/[username]/.claude

# MCP Servers Configuration
# ==========================================
# Comma-separated list of enabled MCP servers
# Servers must be configured in ~/.claude/mcp-servers.json
ENABLED_MCP_SERVERS=playwright

# Claude Agent Configuration
# ==========================================
# AI model to use
CLAUDE_MODEL=claude-opus-4-1-20250805

# Permission mode: default, acceptEdits, bypassPermissions, plan
CLAUDE_PERMISSION_MODE=default

# Allowed tools (comma-separated)
CLAUDE_ALLOWED_TOOLS=WebSearch,WebFetch,Read,Write,Bash

# Disallowed tools (comma-separated, optional)
# CLAUDE_DISALLOWED_TOOLS=

# Application Configuration
# ==========================================
# Default output directory
# OUTPUT_DIR=./output

# Default documentation format
# DEFAULT_FORMAT=markdown

# Default crawling depth
# DEFAULT_DEPTH=2
